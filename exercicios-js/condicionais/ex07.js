// 7. Em uma festa, só entra quem tem convite ou está na lista VIP. Verifique se uma pessoa pode entrar.
let convite = true
let VIP = true

if (convite && VIP) {
    console.log('Entrada permitida!')
} else {
    console.log('Entrada não permitida!')
}
